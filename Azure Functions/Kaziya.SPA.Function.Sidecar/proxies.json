{
  "$schema": "http://json.schemastore.org/proxies",
  "proxies": {
    "ui": {
      "matchCondition": {
        "route": "{*restOfPath}"
      },
      "backendUri": "%BASE_URL%/api/spa/{restOfPath}" // parameter "restOfPath" muss anscheinend den gleichen Namen haben wie die Route
    }
  }
}